<section>
    <div class="wrapper">
        <div class="main-post">
            <div class="post-left">
                <div class="post-details">
                    <h1>{{post.event_title}}</h1>
                    <div class="post-cat">
                    {{!-- CHANGe classes for css --}}
                        <p class="venue">{{post.venue}}</p>
                        <p class="date">{{post.date}}</p>
                        <p class="genre">{{post.genre}}</p>
                        <p class="city">{{post.city}}</p>
                    </div>
                    <p class="desc">{{post.event_description}}</p>
                    <a href="https://www.imdb.com/title/tt4912910/" target="_blank">Read More</a>
                    <div class="social-btn">
                    {{!-- Place holder for NPM music player --}}
                        <button>
                            <i class="bi bi-play"></i> HEAR MUSIC
                        </button>

                    {{!-- Make into a span to display the attendance count? --}}
                        <button>
                            <i class="bi bi-thumbs-up"></i>ATTENDING?
                        </button>

                    {{!-- Proper attending icon  --}}
                        <button>
                            <i class="bi bi-thumbs-up"></i>ATTENDING?
                        </button>
                    </div>	
                </div>
            </div>
            <div class="post-right">
                <div class="img-container">
                    <img src="https://upload.wikimedia.org/wikipedia/en/f/ff/MI_%E2%80%93_Fallout.jpg" alt="">
                </div>
            </div>
        </div>
    </div>
</section>

